services:
  db:
    restart: unless-stopped
    ports: !override
      - "127.0.0.1:5434:5432"

  backend:
    restart: unless-stopped

  frontend:
    restart: unless-stopped
    ports: !override
      - "0.0.0.0:8020:80"
