# TODO
- Add a password confirm for registering new accounts
- Only allow invited users to register, include a one-time invitation code that expires after 7 days, put the code in the URI so that an easy link can be shared, only allow existing users to invite new users. Only allow 5 invitations per day to be sent by each user, but new usrs have to wait only after 24 hours of registering to send invitations. 
- Show a list of invitations and their status on the account menu.
